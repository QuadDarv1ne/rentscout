# 🔍 Настройка Sentry Monitoring

## Обзор

[Sentry](https://sentry.io/) — платформа для мониторинга ошибок и производительности в реальном времени.

---

## 📋 Что отслеживает Sentry

### Ошибки
- Исключения Python (Traceback)
- HTTP ошибки (5xx)
- Ошибки базы данных
- Ошибки Redis
- Ошибки парсеров

### Производительность
- Время ответа API endpoint'ов
- Время выполнения запросов к БД
- Время HTTP запросов к внешним сервисам
- Время выполнения Celery задач

### Release Health
- Стабильность каждой версии
- Количество пользователей затронутых ошибками
- Частота возникновения ошибок

---

## 🚀 Быстрая настройка

### Шаг 1: Регистрация в Sentry

1. Перейдите на [sentry.io](https://sentry.io/)
2. Зарегистрируйтесь (бесплатный тариф включает 5000 ошибок/месяц)
3. Создайте новый проект:
   - Платформа: **Python**
   - Фреймворк: **FastAPI**

### Шаг 2: Получение DSN

После создания проекта вы получите **DSN** (Data Source Name):

```
https://<public_key>@o<org_id>.ingest.sentry.io/<project_id>
```

### Шаг 3: Настройка в .env

Добавьте в ваш `.env` файл:

```env
# Sentry Monitoring
SENTRY_DSN=https://<public_key>@o<org_id>.ingest.sentry.io/<project_id>
```

### Шаг 4: Установка зависимости

```bash
pip install sentry-sdk[fastapi]>=2.0.0
```

Или обновите зависимости:

```bash
pip install -r requirements.txt
```

### Шаг 5: Запуск приложения

Sentry автоматически инициализируется при старте приложения.

Проверьте логи:
```
INFO: Sentry успешно инициализирован (environment=development)
```

---

## ⚙️ Конфигурация

### Переменные окружения

```env
# Обязательная
SENTRY_DSN=https://<key>@o<org>.ingest.sentry.io/<project_id>

# Опционально (для кастомизации)
SENTRY_ENVIRONMENT=production  # development, staging, production
SENTRY_TRACES_SAMPLE_RATE=0.1  # 0.0-1.0, процент транзакций для профилирования
SENTRY_PROFILES_SAMPLE_RATE=0.1  # 0.0-1.0, процент профилей для сбора
```

### Настройка в коде

Файл: `app/utils/sentry.py`

Основные настройки:

```python
sentry_sdk.init(
    dsn=settings.SENTRY_DSN,
    
    # Интеграции
    integrations=[
        FastApiIntegration(),
        LoggingIntegration(),
        RedisIntegration(),
        SqlalchemyIntegration(),
        CeleryIntegration(),
    ],
    
    # Трассировка производительности
    traces_sample_rate=0.1,  # 10% транзакций
    
    # Профилирование
    profiles_sample_rate=0.1,  # 10% профилей
    
    # Окружение
    environment="production",
    
    # Версия приложения
    release=f"rentscout@{version}",
    
    # Безопасность
    send_default_pii=False,  # Не отправлять персональные данные
)
```

---

## 📊 Использование в коде

### Базовое логирование ошибок

Sentry автоматически перехватывает:
- Все необработанные исключения
- Ошибки логирования через `logger.error()`
- HTTP ошибки 5xx

### Ручная отправка ошибок

```python
from app.utils import sentry_utils

# Отправка исключения
try:
    result = risky_operation()
except Exception as e:
    sentry_utils.capture_exception(e, context={
        "operation": "risky_operation",
        "user_id": user_id,
    })
```

### Отправка сообщений

```python
# Информационное сообщение
sentry_utils.capture_message("Важное событие", level="info")

# Предупреждение
sentry_utils.capture_message("Подозрительная активность", level="warning")
```

### Контекст пользователя

```python
from app.utils import sentry_utils

# После аутентификации
sentry_utils.set_user_context(
    user_id=user.id,
    username=user.username
)
```

### Теги и контекст

```python
# Добавление тегов
sentry_utils.set_tag("city", "Москва")
sentry_utils.set_tag("source", "avito")

# Добавление контекста
sentry_utils.set_context("parser", {
    "source": "cian",
    "properties_parsed": 150,
    "duration_seconds": 45.2,
})
```

### Трассировка производительности

```python
from app.utils import sentry_utils

# Ручное создание транзакции
with sentry_utils.start_transaction(name="process_properties", op="parser"):
    properties = parse_all_sources()
    save_to_database(properties)
```

---

## 🔒 Безопасность и фильтрация

### Автоматическая фильтрация

Модуль `sentry.py` автоматически фильтрует:

- ❌ Пароли
- ❌ Токены и API ключи
- ❌ Заголовки Authorization
- ❌ Email и username пользователей
- ❌ Ошибки валидации (4xx)

### Игнорируемые ошибки

Не отправляются в Sentry:

- `ValidationError` (ошибки Pydantic)
- `HTTPException` со статусом 4xx
- Ошибки аутентификации (401)
- Ошибки доступа (403)

### Настройка фильтрации

Измените функцию `before_send_processor()`:

```python
def before_send_processor(event: dict, hint: dict) -> dict | None:
    # Добавить игнорируемые исключения
    ignored_exceptions = [
        "ValidationError",
        "HTTPException",
        "YourCustomException",  # Добавьте своё
    ]
    
    # ... остальной код
```

---

## 📈 Дашборды и алерты

### Создание дашборда

1. Перейдите в **Dashboards** → **Create Dashboard**
2. Добавьте виджеты:
   - **Errors over time** — график ошибок
   - **Error by type** — распределение по типам
   - **Slow transactions** — медленные запросы
   - **Failed tasks** — неудачные Celery задачи

### Настройка алертов

1. Перейдите в **Alerts** → **Create Alert**
2. Выберите правило:
   - **Error Rate** > 1% за 5 минут
   - **Transaction Duration** > 2с за 10 минут
   - **New Issue** — новая ошибка

3. Настройте уведомления:
   - Email
   - Slack
   - Discord
   - Webhook

### Рекомендуемые алерты

```yaml
# Критические ошибки
- Condition: error.rate > 0.05 (5%)
- Window: 5 minutes
- Action: Email + Slack

# Медленные запросы
- Condition: transaction.duration.p95 > 2000ms
- Window: 10 minutes
- Action: Email

# Падение парсеров
- Condition: count(events) > 10 AND tag:parser
- Window: 1 minute
- Action: Slack + Webhook
```

---

## 🧪 Тестирование

### Проверка отправки ошибок

Добавьте тестовый endpoint:

```python
@app.get("/api/test-sentry")
async def test_sentry():
    """Тестовый endpoint для проверки Sentry"""
    raise Exception("Test Sentry error!")
```

Вызовите endpoint и проверьте панель Sentry.

### Проверка в логах

```bash
# Ищите сообщения
grep "Sentry" logs/app.log

# Пример успешной инициализации
INFO: Sentry успешно инициализирован (environment=development)

# Пример отправки ошибки
DEBUG: Исключение отправлено в Sentry: <event_id>
```

---

## 🎯 Best Practices

### ✅ Делайте

- Устанавливайте `traces_sample_rate=0.1` для production
- Фильтруйте чувствительные данные
- Добавляйте контекст к ошибкам
- Используйте теги для группировки
- Настраивайте алерты на критические ошибки

### ❌ Не делайте

- Не отправляйте персональные данные (PII)
- Не логируйте пароли и токены
- Не отправляйте все 4xx ошибки
- Не используйте Sentry для логирования бизнес-событий
- Не забывайте про GDPR и локальные законы о данных

---

## 💰 Тарифы и лимиты

### Free (Бесплатно)

- 5,000 ошибок/месяц
- 10,000 транзакций/месяц
- 7 дней хранения данных
- 1 пользователь

### Team ($26/месяц)

- 50,000 ошибок/месяц
- 100,000 транзакций/месяц
- 30 дней хранения
- Неограниченно пользователей

### Business ($80/месяц)

- 100,000 ошибок/месяц
- 500,000 транзакций/месяц
- 90 дней хранения
- Приоритетная поддержка

---

## 🔗 Полезные ссылки

- [Sentry Python SDK](https://docs.sentry.io/platforms/python/)
- [FastAPI Integration](https://docs.sentry.io/platforms/python/integrations/fastapi/)
- [Performance Monitoring](https://docs.sentry.io/platforms/python/performance/)
- [Release Health](https://docs.sentry.io/product/releases/health/)

---

## 🆘 Troubleshooting

### Sentry не отправляет ошибки

1. Проверьте DSN в `.env`
2. Убедитесь что `sentry-sdk` установлен
3. Проверьте логи на ошибки инициализации
4. Проверьте firewall (Sentry использует HTTPS)

### Слишком много событий

1. Уменьшите `traces_sample_rate`
2. Добавьте фильтрацию в `before_send_processor`
3. Игнорируйте частые ошибки через `ignored_exceptions`

### Ошибки не группируются

1. Проверьте `transaction_style` в настройках
2. Используйте одинаковые теги для похожих ошибок
3. Настройте `fingerprint` для кастомной группировки

---

**Последнее обновление:** 21 февраля 2026 г.
