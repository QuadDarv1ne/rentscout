# 🔐 Аудит Безопасности RentScout

## Дата аудита: 21 февраля 2026 г.

---

## ✅ Выполненные Улучшения

### 1. Rate Limiting для Auth Endpoints

**Проблема:** Отсутствовала защита от brute-force атак на эндпоинты аутентификации.

**Решение:**
- Создан модуль `app/utils/auth_ratelimiter.py`
- Ограничения:
  - **Login:** 5 попыток в минуту (после 3+ — предупреждение, после 5 — бан на 5 минут)
  - **Registration:** 10 попыток в минуту (бан на 10 минут при превышении)
  - **Refresh:** 10 попыток в минуту (бан на 5 минут)
- Автоматическая блокировка IP при превышении лимита
- Логирование подозрительных активностей

**Файлы:**
- `app/utils/auth_ratelimiter.py` — новый модуль
- `app/api/endpoints/auth.py` — обновлён с проверками rate limit

---

### 2. Тесты Безопасности

**Добавлены тесты:**
- `tests/test_auth.py` — 20+ тестов для аутентификации
- Проверка слабых паролей
- Проверка дубликатов username/email
- Проверка запрета регистрации администратором
- Тесты refresh токенов
- Тесты logout

---

### 3. Валидация Секретов

**Существующая валидация в `app/core/config.py`:**
- ✅ Проверка длины SECRET_KEY (минимум 32 символа)
- ✅ Проверка длины JWT_SECRET (минимум 32 символа)
- ✅ Проверка на дефолтные/слабые значения
- ✅ Проверка сложности паролей в DATABASE_URL и REDIS_URL
- ✅ Предупреждения при запуске с небезопасными значениями

---

## ⚠️ Критические Требования для Production

### 1. Генерация Безопасных Ключей

**Перед запуском в production выполните:**

```bash
python scripts/generate_secrets.py
```

Это создаст `.env` файл со следующими параметрами:
- `SECRET_KEY` — 64 символа, криптографически стойкий
- `JWT_SECRET` — 64 символа, криптографически стойкий
- `POSTGRES_PASSWORD` — 32 символа, сложный пароль
- `REDIS_PASSWORD` — 32 символа, сложный пароль

**Или вручную:**

```bash
# Для Linux/Mac
openssl rand -hex 32  # Для SECRET_KEY
openssl rand -hex 32  # Для JWT_SECRET

# Для Windows (PowerShell)
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | ForEach-Object {[char]$_})
```

---

### 2. Проверка Переменных Окружения

**Минимальный набор для production:**

```env
# Security - ОБЯЗАТЕЛЬНО замените!
SECRET_KEY=<64+ символа, сгенерируйте скриптом>
JWT_SECRET=<64+ символа, сгенерируйте скриптом>

# Database
DATABASE_URL=postgresql://user:<secure_password>@host:5432/rentscout
POSTGRES_PASSWORD=<32+ символа>

# Redis
REDIS_URL=redis://:<secure_password>@host:6379/0
REDIS_PASSWORD=<32+ символа>

# HTTPS (рекомендуется)
HTTPS_ENABLED=true
SSL_CERT_FILE=/path/to/cert.pem
SSL_KEY_FILE=/path/to/key.pem

# CORS (укажите ваши домены)
ALLOWED_ORIGINS=https://yourdomain.com

# Sentry (для мониторинга ошибок)
SENTRY_DSN=https://<key>@sentry.io/<project_id>
```

---

### 3. HTTPS Настройка

**Для включения HTTPS:**

1. Получите SSL сертификат (Let's Encrypt бесплатно):
   ```bash
   certbot certonly --standalone -d yourdomain.com
   ```

2. Настройте в `.env`:
   ```env
   HTTPS_ENABLED=true
   SSL_CERT_FILE=/etc/letsencrypt/live/yourdomain.com/fullchain.pem
   SSL_KEY_FILE=/etc/letsencrypt/live/yourdomain.com/privkey.pem
   ```

3. Включите HSTS заголовок:
   ```env
   HSTS_ENABLED=true
   ```

---

### 4. Брандмауэр и Сеть

**Рекомендуемые правила:**

```bash
# Разрешить только необходимые порты
ufw allow 80/tcp    # HTTP (для редиректа)
ufw allow 443/tcp   # HTTPS
ufw allow 22/tcp    # SSH (ограничьте по IP)

# Заблокировать прямой доступ к БД и Redis
ufw deny 5432/tcp
ufw deny 6379/tcp

# Включить брандмауэр
ufw enable
```

---

## 🔍 Чеклист Безопасности Production

### Перед Деплоем

- [ ] Сгенерированы уникальные SECRET_KEY и JWT_SECRET
- [ ] Пароли БД и Redis сложные (32+ символа)
- [ ] `.env` файл добавлен в `.gitignore`
- [ ] `.env` не попал в репозиторий (проверьте `git ls-files`)
- [ ] Включён HTTPS
- [ ] Настроен CORS для конкретных доменов
- [ ] Rate limiting включён и настроен
- [ ] Логирование настроено (уровень INFO или WARNING)
- [ ] Sentry или аналог подключён для мониторинга ошибок

### После Деплоя

- [ ] Проверка `/api/health` возвращает 200
- [ ] Проверка `/api/auth/status` работает
- [ ] Rate limiting работает (попробуйте 6+ неудачных login)
- [ ] Логи пишутся в `/logs`
- [ ] Метрики Prometheus доступны на `/metrics`
- [ ] Нет ошибок в логах при старте

### Регулярные Проверки

- [ ] Раз в месяц: проверка логов на подозрительную активность
- [ ] Раз в квартал: обновление зависимостей (`pip install -U -r requirements.txt`)
- [ ] Раз в квартал: проверка обновлений безопасности
- [ ] Раз в год: смена SECRET_KEY и JWT_SECRET (требуется перезапуск)

---

## 🚨 Мониторинг Подозрительной Активности

### Логи для Проверки

**Файл:** `logs/app.log`

**Ищите:**
```
WARNING ... Неудачная попытка входа
WARNING ... Множественные попытки входа с IP
WARNING ... IP забанен за чрезмерные попытки аутентификации
WARNING ... Rate limit exceeded
```

### Команды для Анализа

```bash
# Неудачные попытки входа
grep "Неудачная попытка входа" logs/app.log | tail -20

# Забаненные IP
grep "IP забанен" logs/app.log | tail -10

# Превышения rate limit
grep "Rate limit exceeded" logs/app.log | wc -l
```

---

## 📊 Статистика Безопасности

### Текущие Лимиты

| Endpoint | Лимит | Окно | Бан при превышении |
|----------|-------|------|-------------------|
| `/api/auth/login` | 5 попыток | 60 сек | 5 минут |
| `/api/auth/register` | 10 попыток | 60 сек | 10 минут |
| `/api/auth/refresh` | 10 попыток | 60 сек | 5 минут |
| Общий API (IP) | 100 запросов | 60 сек | Нет (429) |
| Burst (IP) | 10 запросов | 1 сек | Нет (429) |

---

## 🔧 Дополнительные Меры (Опционально)

### 1. Двухфакторная Аутентификация (2FA)

**Требуется добавить:**
- TOTP (Google Authenticator)
- SMS верификация
- Email верификация

### 2. Blacklist Токенов

**Для отзыва токенов:**
```python
# При logout добавлять токен в Redis blacklist
await redis.setex(f"blacklist:{access_token}", ttl, 1)
```

### 3. Аудит Действий Пользователей

**Логировать:**
- Входы/выходы
- Изменения профиля
- Изменения ролей (админ действия)

### 4. Content Security Policy (CSP)

**Настройка заголовков:**
```python
response.headers["Content-Security-Policy"] = "default-src 'self'"
```

---

## 📞 Экстренные Действия

### При Взломе/Утечке

1. **Немедленно смените секреты:**
   ```bash
   python scripts/generate_secrets.py
   docker-compose restart
   ```

2. **Заблокируйте подозрительные IP:**
   ```bash
   # Добавьте в firewall
   ufw deny from <suspicious_ip>
   ```

3. **Отозовите все токены:**
   - Очистите Redis blacklist
   - Принудительно разлогиньте всех пользователей

4. **Проверьте логи:**
   - Найдите источник атаки
   - Оцените ущерб

5. **Уведомите пользователей** (если данные скомпрометированы)

---

## 📚 Дополнительные Ресурсы

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [FastAPI Security](https://fastapi.tiangolo.com/tutorial/security/)
- [Python Security Best Practices](https://snyk.io/blog/python-security-best-practices/)

---

**Контакты для сообщений об уязвимостях:** security@rentscout.dev
