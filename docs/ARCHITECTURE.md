# 🏗️ Архитектура RentScout

**Версия:** 2.3.0  
**Последнее обновление:** 21 февраля 2026 г.

---

## 📋 Оглавление

1. [Обзор системы](#обзор-системы)
2. [Архитектурная диаграмма](#архитектурная-диаграмма)
3. [Компоненты](#компоненты)
4. [Поток данных](#поток-данных)
5. [Технологический стек](#технологический-стек)
6. [Масштабирование](#масштабирование)

---

## Обзор системы

RentScout — это распределённая система для агрегации объявлений об аренде недвижимости с множественных источников.

### Основные возможности

- **Парсинг** — сбор данных с 10+ площадок (Avito, Cian, DomClick, и др.)
- **Поиск** — расширенная фильтрация (15+ параметров)
- **Кеширование** — многоуровневое кеширование (Redis + in-memory)
- **ML** — предсказание цен, анализ трендов
- **Уведомления** — email, WebSocket, Telegram
- **Закладки** — управление избранными объявлениями

---

## Архитектурная диаграмма

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            Client Layer                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐                │
│  │   Web    │  │  Mobile  │  │   CLI    │  │  Admin   │                │
│  │   App    │  │   App    │  │   Tool   │  │  Panel   │                │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘                │
└───────┼─────────────┼─────────────┼─────────────┼───────────────────────┘
        │             │             │             │
        └─────────────┴──────┬──────┴─────────────┘
                             │
                    ┌────────▼────────┐
                    │   Nginx (LB)    │
                    │  + SSL/TLS      │
                    └────────┬────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
┌───────▼────────┐  ┌───────▼────────┐  ┌───────▼────────┐
│   FastAPI #1   │  │   FastAPI #2   │  │   FastAPI #3   │
│   (Uvicorn)    │  │   (Uvicorn)    │  │   (Uvicorn)    │
└───────┬────────┘  └───────┬────────┘  └───────┬────────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
┌───────▼────────┐  ┌───────▼────────┐  ┌──────▼────────┐
│    PostgreSQL  │  │     Redis      │  │  Elasticsearch│
│    (Primary)   │  │    (Cache)     │  │    (Search)   │
└────────────────┘  └────────────────┘  └───────────────┘

        ┌──────────────────────────────────────────┐
        │          Background Workers              │
        │  ┌────────────┐     ┌────────────┐       │
        │  │   Celery   │     │   Celery   │       │
        │  │  Worker #1 │     │  Worker #2 │       │
        │  └─────┬──────┘     └─────┬──────┘       │
        │        │                  │               │
        │  ┌─────▼──────┐     ┌─────▼──────┐       │
        │  │  Parsers   │     │  Parsers   │       │
        │  │  (Avito)   │     │   (Cian)   │       │
        │  └────────────┘     └────────────┘       │
        └──────────────────────────────────────────┘

        ┌──────────────────────────────────────────┐
        │          Monitoring & Observability      │
        │  ┌────────────┐     ┌────────────┐       │
        │  │ Prometheus │     │   Grafana  │       │
        │  │  (Metrics) │     │  (Dashboard)│      │
        │  └────────────┘     └────────────┘       │
        │  ┌────────────┐     ┌────────────┐       │
        │  │   Sentry   │     │   Flower   │       │
        │  │  (Errors)  │     │  (Celery)  │       │
        │  └────────────┘     └────────────┘       │
        └──────────────────────────────────────────┘
```

---

## Компоненты

### 1. API Layer (FastAPI)

**Файл:** `app/main.py`

**Ответственность:**
- Обработка HTTP запросов
- Аутентификация и авторизация
- Валидация входных данных
- Rate limiting
- Логирование

**Структура:**
```
app/
├── api/
│   ├── endpoints/       # API endpoint'ы
│   ├── v1/              # Версия API v1
│   ├── deps.py          # Зависимости (dependencies)
│   └── router_registration.py  # Регистрация роутеров
├── core/
│   ├── config.py        # Конфигурация
│   └── security.py      # Безопасность (JWT, пароли)
├── db/
│   ├── models/          # SQLAlchemy модели
│   └── repositories/    # Доступ к данным
├── parsers/
│   ├── base_parser.py   # Базовый класс парсера
│   ├── avito/           # Avito парсер
│   ├── cian/            # Cian парсер
│   └── ...
├── services/
│   ├── search.py        # Поиск объявлений
│   ├── cache.py         # Кеширование
│   └── notifications.py # Уведомления
├── tasks/
│   ├── celery.py        # Celery конфигурация
│   └── parsers.py       # Фоновые задачи парсинга
└── utils/
    ├── logger.py        # Логирование
    ├── sentry.py        # Sentry интеграция
    └── json_logger.py   # JSON логи
```

---

### 2. Database Layer

#### PostgreSQL

**Назначение:** Хранение объявлений, пользователей, закладок

**Основные таблицы:**
- `properties` — объявления
- `bookmarks` — закладки пользователей
- `ml_price_history` — история ML предсказаний
- `users` — пользователи (если будет добавлено)

**Схема:**
```sql
-- Properties
CREATE TABLE properties (
    id SERIAL PRIMARY KEY,
    external_id VARCHAR(255) UNIQUE,
    title VARCHAR(500),
    price INTEGER,
    rooms INTEGER,
    area FLOAT,
    city VARCHAR(100),
    district VARCHAR(100),
    source VARCHAR(50),
    url TEXT,
    description TEXT,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- Bookmarks
CREATE TABLE bookmarks (
    id SERIAL PRIMARY KEY,
    user_id VARCHAR(100),
    external_property_id VARCHAR(255),
    property_title VARCHAR(500),
    bookmark_type VARCHAR(20),
    collection_name VARCHAR(100),
    notes TEXT,
    tags TEXT[],
    rating INTEGER,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

#### Redis

**Назначение:**
- Кеширование результатов поиска
- Rate limiting
- Сессии пользователей
- Блокировки (distributed locks)

**Структура ключей:**
```
cache:search:{hash}          # Кэш поиска
ratelimit:auth:{ip}          # Rate limit для auth
ratelimit:global:{ip}        # Общий rate limit
blacklist:token:{jti}        # Blacklist токенов
lock:parser:{source}         # Блокировка парсера
```

---

### 3. Parser Layer

**Базовый класс:** `app/parsers/base_parser.py`

**Иерархия:**
```
BaseParser (async)
├── AvitoParser
├── CianParser
├── DomClickParser
├── DomofondParser
├── YandexRealtyParser
├── SutochnoParser
├── TvilParser
└── ...
```

**Жизненный цикл парсинга:**
1. Получение списка URL для парсинга
2. Проверка rate limit
3. HTTP запрос с retry логикой
4. Парсинг HTML/JSON
5. Валидация данных
6. Сохранение в БД (batch insert)
7. Обновление кэша

---

### 4. Background Tasks (Celery)

**Конфигурация:** `app/tasks/celery.py`

**Задачи:**
- `parse_source(source, params)` — парсинг источника
- `cache_warmup()` — предзагрузка кэша
- `send_notifications()` — отправка уведомлений
- `cleanup_old_data()` — очистка старых данных

**Расписание (Celery Beat):**
```python
CELERY_BEAT_SCHEDULE = {
    "parse-avito-every-hour": {
        "task": "app.tasks.parsers.parse_avito",
        "schedule": crontab(minute=0),
    },
    "parse-cian-every-hour": {
        "task": "app.tasks.parsers.parse_cian",
        "schedule": crontab(minute=30),
    },
    "cache-warmup-every-day": {
        "task": "app.tasks.cache_maintenance.cache_warmer",
        "schedule": crontab(hour=3, minute=0),
    },
}
```

---

### 5. ML Layer

**Модуль:** `app/ml/`

**Компоненты:**
- `price_predictor.py` — предсказание цен
- `market_analyzer.py` — анализ рынка
- `trend_detector.py` — обнаружение трендов

**ML Pipeline:**
```
1. Загрузка исторических данных из БД
2. Предобработка (feature engineering)
3. Применение модели (градиентный бустинг / линейная регрессия)
4. Расчёт confidence interval
5. Кэширование результата
6. Возврат предсказания
```

---

## Поток данных

### Поиск объявлений

```
Client Request
     │
     ▼
┌─────────────────┐
│  API Gateway    │
│  (Rate Limit)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Search Service │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌──────┐  ┌──────┐
│ Cache│  │  DB  │
│  ✓   │  │      │
└──┬───┘  └──────┘
   │
   │ Hit
   ▼
Return Response

   │
   │ Miss
   ▼
┌─────────────────┐
│  Parse Sources  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Save to Cache  │
└────────┬────────┘
         │
         ▼
Return Response
```

---

### Добавление в закладки

```
Client Request (POST /api/bookmarks/add)
     │
     ▼
┌─────────────────┐
│  Auth Check     │
│  (JWT Verify)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Validate Data  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Check Duplicate│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Insert to DB   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Update Cache   │
└────────┬────────┘
         │
         ▼
Return Success
```

---

## Технологический стек

### Backend

| Компонент | Технология | Версия |
|-----------|------------|--------|
| Framework | FastAPI | 0.100+ |
| Language | Python | 3.9+ |
| ASGI Server | Uvicorn | 0.23+ |
| ORM | SQLAlchemy | 2.0+ |
| Migrations | Alembic | 1.13+ |

### Database

| Компонент | Технология | Версия |
|-----------|------------|--------|
| Primary DB | PostgreSQL | 15 |
| Cache | Redis | 7 |
| Search (opt) | Elasticsearch | 8 |

### Background Tasks

| Компонент | Технология | Версия |
|-----------|------------|--------|
| Queue | Celery | 5.3+ |
| Broker | Redis | 7 |
| Monitor | Flower | 2.0 |

### Monitoring

| Компонент | Технология | Версия |
|-----------|------------|--------|
| Metrics | Prometheus | 2.x |
| Dashboard | Grafana | 10.x |
| Errors | Sentry | Latest |
| Logs | Structlog + ELK/Loki | - |

### Infrastructure

| Компонент | Технология | Версия |
|-----------|------------|--------|
| Container | Docker | 24+ |
| Orchestration | Docker Compose | 2.x |
| Reverse Proxy | Nginx | 1.25+ |
| CI/CD | GitHub Actions | - |

---

## Масштабирование

### Горизонтальное масштабирование API

```yaml
# docker-compose.prod.yml
services:
  web:
    deploy:
      replicas: 4
      resources:
        limits:
          cpus: '2'
          memory: 2G
```

### Масштабирование парсеров

- Каждый парсер запускается в отдельном Celery worker
- Rate limit настраивается индивидуально для каждого источника
- При блокировке — автоматическая ротация прокси/USER-AGENT

### Кэширование

**Многоуровневое:**
1. **L1** — In-memory (app_cache) — 1-5 минут
2. **L2** — Redis (advanced_cache) — 5-60 минут
3. **L3** — Database — постоянно

---

## Безопасность

### Защита API

- JWT аутентификация
- Rate limiting (IP + user)
- CORS (restrict origins)
- HTTPS (production)
- Security headers (HSTS, CSP)

### Защита данных

- Хеширование паролей (Argon2)
- Шифрование чувствительных данных
- Audit log действий
- Backup базы данных

---

## Ссылки

- [Onboarding Guide](ONBOARDING.md) — для новых разработчиков
- [Runbook](RUNBOOK.md) — действия при инцидентах
- [API Examples](API_EXAMPLES.md) — примеры запросов
- [Security Audit](SECURITY_AUDIT.md) — аудит безопасности

---

**Поддерживаемые версии:**
- Python: 3.9, 3.10, 3.11
- PostgreSQL: 14, 15
- Redis: 6, 7
