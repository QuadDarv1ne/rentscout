# 🚨 Runbook — Действия при инцидентах

**Версия:** 1.0  
**Последнее обновление:** 21 февраля 2026 г.

---

## 📋 Оглавление

1. [Критические инциденты](#критические-инциденты)
2. [Диагностика](#диагностика)
3. [Восстановление](#восстановление)
4. [Post-mortem](#post-mortem)
5. [Контакты](#контакты)

---

## Критические инциденты

### 🔴 Уровень 1 (Critical)

**Симптомы:**
- API полностью недоступен (5xx на все запросы)
- База данных недоступна
- Потеря данных
- Утечка секретов/паролей

**Время реакции:** 15 минут  
**Время восстановления:** 2 часа

---

### 🟠 Уровень 2 (High)

**Симптомы:**
- Частичная недоступность API
- Парсеры не работают > 1 часа
- Кэш полностью очищен
- Sentry показывает > 100 ошибок/минуту

**Время реакции:** 30 минут  
**Время восстановления:** 4 часа

---

### 🟡 Уровень 3 (Medium)

**Симптомы:**
- Замедление ответов API (> 5с)
- Отдельные парсеры не работают
- Метрики Prometheus недоступны

**Время реакции:** 2 часа  
**Время восстановления:** 24 часа

---

## Диагностика

### 1. Проверка статуса сервисов

```bash
# Быстрая проверка здоровья
curl http://localhost:8000/api/health

# Детальная проверка
curl http://localhost:8000/api/health/detailed

# Проверка метрик
curl http://localhost:8000/metrics
```

### 2. Проверка Docker сервисов

```bash
# Статус всех сервисов
docker-compose ps

# Логи API
docker logs rentscout-api --tail 100 -f

# Логи базы данных
docker logs rentscout-postgres --tail 100

# Логи Redis
docker logs rentscout-redis --tail 100

# Логи Celery
docker logs rentscout-celery-worker --tail 100
```

### 3. Проверка базы данных

```bash
# Подключение к БД
make db-shell

# Проверка количества подключений
SELECT count(*) FROM pg_stat_activity;

# Проверка блокировок
SELECT * FROM pg_locks WHERE NOT granted;

# Проверка размера базы
SELECT pg_size_pretty(pg_database_size('rentscout'));
```

### 4. Проверка Redis

```bash
# Подключение к Redis
docker exec -it rentscout-redis redis-cli -a redis_password

# Проверка статуса
INFO

# Проверка использования памяти
INFO memory

# Проверка ключей
DBSIZE

# Проверка медленных запросов
SLOWLOG GET 10
```

### 5. Проверка метрик

**Prometheus:** http://localhost:9091

**Полезные запросы:**
```promql
# Количество ошибок в секунду
rate(http_requests_total{status=~"5.."}[5m])

# Время ответа API
histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m]))

# Использование памяти
container_memory_usage_bytes{container="rentscout-api"}

# Количество подключений к БД
pg_stat_activity_count
```

**Grafana:** http://localhost:3001

---

## Восстановление

### 1. Перезапуск сервисов

```bash
# Перезапуск API
docker-compose restart web

# Перезапуск всех сервисов
docker-compose restart

# Полная пересборка
make docker-rebuild
```

### 2. Откат к предыдущей версии

```bash
# Если проблема в новом деплое
git revert HEAD
git push origin main

# Или откат Docker образа
docker-compose pull
docker-compose up -d
```

### 3. Восстановление базы данных

```bash
# Создание бэкапа (если ещё доступен)
make db-backup

# Восстановление из бэкапа
make db-restore

# Полная очистка и миграция (крайняя мера!)
make db-reset
```

### 4. Очистка кэша

```bash
# Подключение к Redis
docker exec -it rentscout-redis redis-cli -a redis_password

# Очистка всех ключей (ОПАСНО!)
FLUSHALL

# Очистка ключей по паттерну
KEYS cache:* | xargs redis-cli -a redis_password DEL
```

### 5. Остановка парсеров (если проблема в них)

```bash
# Остановка Celery worker
docker-compose stop celery-worker

# Перезапуск с очисткой очереди
docker exec -it rentscout-redis redis-cli -a redis_password FLUSHDB
docker-compose start celery-worker
```

---

## Сценарии восстановления

### Сценарий 1: API не отвечает

**Диагноз:**
```bash
curl http://localhost:8000/api/health
# Connection refused или 502 Bad Gateway
```

**Действия:**
1. Проверить статус Docker контейнеров
   ```bash
   docker-compose ps
   ```

2. Проверить логи API
   ```bash
   docker logs rentscout-api --tail 200
   ```

3. Перезапустить API
   ```bash
   docker-compose restart web
   ```

4. Если не помогло — пересобрать
   ```bash
   make docker-rebuild
   ```

---

### Сценарий 2: База данных недоступна

**Диагноз:**
```bash
docker exec rentscout-postgres pg_isready
# postgres:5432 - no response
```

**Действия:**
1. Проверить статус PostgreSQL
   ```bash
   docker logs rentscout-postgres --tail 200
   ```

2. Проверить место на диске
   ```bash
   docker exec rentscout-postgres df -h
   ```

3. Перезапустить PostgreSQL
   ```bash
   docker-compose restart postgres
   ```

4. Проверить подключения
   ```bash
   make db-shell
   SELECT count(*) FROM pg_stat_activity;
   ```

---

### Сценарий 3: Redis переполнен

**Диагноз:**
```bash
docker exec rentscout-redis redis-cli -a redis_password INFO memory
# used_memory_human: 2GB (больше лимита)
```

**Действия:**
1. Проверить ключи
   ```bash
   docker exec rentscout-redis redis-cli -a redis_password DBSIZE
   ```

2. Найти самые большие ключи
   ```bash
   docker exec rentscout-redis redis-cli -a redis_password --bigkeys
   ```

3. Очистить старые ключи
   ```bash
   docker exec rentscout-redis redis-cli -a redis_password KEYS "cache:*" | head -1000 | xargs redis-cli -a redis_password DEL
   ```

4. Перезапустить Redis
   ```bash
   docker-compose restart redis
   ```

---

### Сценарий 4: Утечка секретов

**Действия:**
1. **Немедленно** сменить все секреты
   ```bash
   python scripts/generate_secrets.py
   ```

2. Обновить `.env` и перезапустить сервисы
   ```bash
   docker-compose down
   docker-compose up -d
   ```

3. Отозвать все JWT токены
   ```bash
   docker exec rentscout-redis redis-cli -a redis_password FLUSHDB
   ```

4. Проверить логи на несанкционированный доступ
   ```bash
   grep "authenticated" logs/app.log | tail 100
   ```

5. Уведомить пользователей (если затронуты персональные данные)

---

### Сценарий 5: DDoS атака

**Диагноз:**
- Резкий рост трафика (> 1000 RPS)
- Rate limit не справляется
- API замедляется

**Действия:**
1. Включить строгий rate limiting
   ```env
   # В .env
   API_RATE_LIMIT=10
   RATE_LIMIT_BURST=5
   ```

2. Перезапустить API
   ```bash
   docker-compose restart web
   ```

3. Заблокировать атакующие IP (если известны)
   ```bash
   # В nginx.conf
   deny 192.168.1.100;
   ```

4. Включить Cloudflare/аналогичный сервис

5. Увеличить ресурсы
   ```yaml
   # В docker-compose.prod.yml
   deploy:
     resources:
       limits:
         cpus: '4'
         memory: 4G
   ```

---

## Post-mortem

### Шаблон отчёта

```markdown
# Post-mortem: [Название инцидента]

**Дата:** YYYY-MM-DD  
**Длительность:** X часов Y минут  
**Уровень:** Critical/High/Medium

## Хронология

- HH:MM — Обнаружена проблема
- HH:MM — Начато расследование
- HH:MM — Найден корень проблемы
- HH:MM — Начато восстановление
- HH:MM — Сервис восстановлен

## Корневая причина

[Описание причины]

## Воздействие

- Пользователей затронуто: X
- Потеря данных: Да/Нет
- Финансовые потери: X руб.

## Принятые меры

1. [Мера 1]
2. [Мера 2]

## Предотвращение в будущем

- [ ] [Задача 1]
- [ ] [Задача 2]
- [ ] [Задача 3]

## Уроки

**Что сработало хорошо:**
- ...

**Что можно улучшить:**
- ...
```

---

## Контакты

### Экстренные контакты

| Роль | Имя | Телефон | Telegram |
|------|-----|---------|----------|
| On-call Engineer | TBD | +7 XXX | @username |
| Tech Lead | TBD | +7 XXX | @username |
| DevOps | TBD | +7 XXX | @username |

### Каналы связи

- **Slack:** #incidents
- **Telegram:** RentScout Alerts
- **Email:** emergency@rentscout.dev

### Эскалация

```
Level 1 (0-15 мин): On-call Engineer
Level 2 (15-30 мин): Tech Lead
Level 3 (30+ мин): CTO / VP Engineering
```

---

## Приложения

### A. Полезные команды

```bash
# Полная диагностика
make health-check

# Бэкап всего
make backup-all

# Экстренная очистка
make emergency-clean
```

### B. Скрипты

**scripts/emergency_restart.sh:**
```bash
#!/bin/bash
echo "🚨 Emergency restart..."
docker-compose down
docker system prune -f
docker-compose up -d
echo "✅ Restart complete"
```

### C. Чеклист после инцидента

- [ ] Сервис полностью восстановлен
- [ ] Все тесты проходят
- [ ] Метрики в норме
- [ ] Пользователи уведомлены (если нужно)
- [ ] Post-mortem написан
- [ ] Задачи на предотвращение созданы
- [ ] Документация обновлена

---

**Помните: главное — не паниковать и следовать инструкциям! 🧘**
