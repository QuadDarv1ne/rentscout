# Contributing to RentScout

Спасибо за интерес к проекту RentScout! 🎉

Мы рады любому вкладу - от исправления опечаток до новых функций.

## 📋 Содержание

- [Code of Conduct](#code-of-conduct)
- [Как помочь проекту](#как-помочь-проекту)
- [Настройка окружения](#настройка-окружения)
- [Процесс разработки](#процесс-разработки)
- [Стандарты кода](#стандарты-кода)
- [Тестирование](#тестирование)
- [Отправка Pull Request](#отправка-pull-request)

## Code of Conduct

Участвуя в этом проекте, вы соглашаетесь соблюдать профессиональное и уважительное поведение.

## Как помочь проекту

### 🐛 Сообщить об ошибке

1. Проверьте, нет ли уже подобного issue
2. Создайте новый issue с:
   - Описанием проблемы
   - Шагами для воспроизведения
   - Ожидаемым поведением
   - Фактическим поведением
   - Версией Python, ОС, зависимостей

### 💡 Предложить улучшение

1. Создайте issue с тегом `enhancement`
2. Опишите предлагаемую функциональность
3. Объясните, зачем это нужно
4. Приведите примеры использования

### 🔧 Написать код

1. Найдите issue с тегом `good first issue` или `help wanted`
2. Прокомментируйте, что хотите взяться за задачу
3. Следуйте процессу разработки ниже

## Настройка окружения

### 1. Fork и клонирование

```bash
# Fork репозиторий на GitHub

# Клонируйте ваш fork
git clone https://github.com/YOUR_USERNAME/rentscout.git
cd rentscout

# Добавьте upstream remote
git remote add upstream https://github.com/QuadDarv1ne/rentscout.git
```

### 2. Создайте виртуальное окружение

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/macOS
source venv/bin/activate
```

### 3. Установите зависимости

```bash
# Основные зависимости
pip install -r requirements.txt

# Зависимости для разработки
pip install -r requirements-dev.txt

# Зависимости для тестирования
pip install -r requirements-test.txt
```

### 4. Настройте pre-commit hooks

```bash
pip install pre-commit
pre-commit install
```

### 5. Настройте окружение

```bash
cp .env.example .env
# Отредактируйте .env под ваши нужды
```

### 6. Запустите базы данных (Docker)

```bash
docker-compose -f docker-compose.dev.yml up -d postgres redis
```

### 7. Выполните миграции

```bash
alembic upgrade head
```

## Процесс разработки

### 1. Создайте ветку для работы

```bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/bug-description
```

Используйте префиксы:
- `feature/` - новая функциональность
- `fix/` - исправление бага
- `docs/` - документация
- `refactor/` - рефакторинг
- `test/` - тесты
- `chore/` - рутинные задачи

### 2. Пишите код

- Следуйте стандартам кода (см. ниже)
- Добавляйте docstrings к функциям и классам
- Комментируйте сложную логику
- Пишите тесты для новой функциональности

### 3. Коммитьте изменения

```bash
git add .
git commit -m "feat: add new feature"
```

Формат коммитов (Conventional Commits):
- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - документация
- `style:` - форматирование
- `refactor:` - рефакторинг
- `test:` - тесты
- `chore:` - рутинные задачи

### 4. Синхронизируйте с upstream

```bash
git fetch upstream
git rebase upstream/master
```

### 5. Push в ваш fork

```bash
git push origin feature/your-feature-name
```

## Стандарты кода

### Форматирование

```bash
# Black для форматирования
black app/ --line-length=120

# isort для сортировки импортов
isort app/ --profile=black --line-length=120
```

### Линтинг

```bash
# Flake8
flake8 app/ --max-line-length=120

# MyPy для проверки типов
mypy app/ --ignore-missing-imports

# Pylint
pylint app/ --max-line-length=120
```

### Стиль кода

- **PEP 8**: Следуйте PEP 8 с длиной строки 120 символов
- **Type Hints**: Используйте type hints везде, где возможно
- **Docstrings**: Google-style docstrings для всех публичных функций/классов
- **Naming**:
  - `snake_case` для функций и переменных
  - `PascalCase` для классов
  - `UPPER_CASE` для констант

### Пример кода

```python
from typing import Optional, List
from pydantic import BaseModel


class Property(BaseModel):
    """Модель объявления о недвижимости.
    
    Attributes:
        title: Заголовок объявления
        price: Цена в рублях
        city: Город
        rooms: Количество комнат
    """
    
    title: str
    price: int
    city: str
    rooms: Optional[int] = None


def search_properties(
    city: str,
    price_max: Optional[int] = None,
    rooms: Optional[int] = None
) -> List[Property]:
    """Поиск объявлений по критериям.
    
    Args:
        city: Название города
        price_max: Максимальная цена
        rooms: Количество комнат
        
    Returns:
        Список найденных объявлений
        
    Raises:
        ValueError: Если город не указан
    """
    if not city:
        raise ValueError("City is required")
    
    # Implementation here
    return []
```

## Тестирование

### Запуск тестов

```bash
# Все тесты
pytest

# С покрытием
pytest --cov=app --cov-report=html

# Конкретный файл
pytest app/tests/test_api.py -v

# Конкретный тест
pytest app/tests/test_api.py::test_search_properties -v
```

### Написание тестов

- Каждая новая функция должна иметь тесты
- Покрытие кода > 80%
- Используйте фикстуры pytest
- Тестируйте граничные случаи

Пример теста:

```python
import pytest
from app.services.search import SearchService


@pytest.fixture
def search_service():
    return SearchService()


def test_search_properties_with_city(search_service):
    """Тест поиска объявлений по городу."""
    results = search_service.search(city="Москва")
    
    assert len(results) > 0
    assert all(p.city == "Москва" for p in results)


def test_search_properties_empty_city(search_service):
    """Тест поиска с пустым городом."""
    with pytest.raises(ValueError):
        search_service.search(city="")
```

## Отправка Pull Request

### Checklist перед PR

- [ ] Код следует стандартам проекта
- [ ] Все тесты проходят
- [ ] Добавлены новые тесты (если нужно)
- [ ] Документация обновлена
- [ ] Pre-commit hooks проходят
- [ ] Коммиты следуют Conventional Commits
- [ ] Ветка синхронизирована с upstream/master

### Создание PR

1. Перейдите на GitHub в ваш fork
2. Нажмите "New Pull Request"
3. Выберите ветку для мержа
4. Заполните шаблон PR:
   - Описание изменений
   - Связанные issues
   - Скриншоты (если UI)
   - Как тестировать

### Шаблон PR

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation update

## Связанные Issues
Fixes #123

## Как тестировать
1. Шаг 1
2. Шаг 2
3. Ожидаемый результат

## Checklist
- [ ] Тесты проходят
- [ ] Код следует стандартам
- [ ] Документация обновлена
- [ ] Self-review выполнен
```

### Review процесс

1. Maintainer проверит ваш PR
2. Возможны запросы на изменения
3. После одобрения PR будет смержен
4. Ваш вклад появится в CHANGELOG

## Структура проекта

```
rentscout/
├── app/
│   ├── api/           # API endpoints
│   ├── core/          # Конфигурация
│   ├── db/            # База данных
│   ├── models/        # Pydantic модели
│   ├── parsers/       # Парсеры источников
│   ├── services/      # Бизнес-логика
│   ├── tasks/         # Celery задачи
│   ├── tests/         # Тесты
│   └── utils/         # Утилиты
├── alembic/           # Миграции БД
├── docker/            # Docker конфигурация
├── docs/              # Документация
└── scripts/           # Вспомогательные скрипты
```

## Вопросы?

- Создайте issue с тегом `question`
- Проверьте [документацию](docs/)
- Изучите [примеры кода](app/tests/)

## Благодарности

Спасибо за ваш вклад в RentScout! 🙏

Каждый PR, issue, и обсуждение помогают сделать проект лучше.
